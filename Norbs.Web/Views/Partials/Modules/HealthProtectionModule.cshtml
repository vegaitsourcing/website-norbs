@inherits UmbracoViewPage<Norbs.Models.Generated.HealthProtectionModule>

@if (!string.IsNullOrWhiteSpace(Model.Title))
{
    @Model.Title
}

<img src="@Model.Image.MediaUrl()"/>

@if (Model.Text != null) {
    @Model.Text
}