<div class="heading-block">
	<div class="wrap wrap--inner">
		{{#if title}}
			<h2 class="h2">{{title}}</h2>
		{{/if}}
		{{#if date}}
			<span class="heading-block__date">{{date}}</span>
		{{/if}}
	</div>
</div>
